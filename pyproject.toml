[project]
name = "studentassistent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.2",
    "django-debug-toolbar>=6.2.0",
    "django-fernet-fields>=0.6",
    "django-htmx>=1.27.0",
    "django-tailwind-cli>=4.5.1",
    "pillow>=12.1.1",
]

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
    "ruff>=0.15.2",
]
